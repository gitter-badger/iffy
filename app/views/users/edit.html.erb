Edit your profile

<%= form_for @user do |u| %>
	<%= u.label :name %> 
	<%= u.text_field :name %>

	<%= u.label :email %> 
	<%= u.text_field :email %>

	<%= u.label :zip %> 
	<%= u.text_field :zip %>

	<%= u.label :radius %> 
	<%= u.text_field :radius %>

	<%= u.submit "Edit Profile" %> 
<% end %>
